/*
 * Copyright (C) 2007 TopCoder Inc., All Rights Reserved.
 *
 * AbstractAddGraphElementActionImpl.java
 */
package com.topcoder.uml.actions.auxiliary.failuretests.impls;

import com.topcoder.uml.actions.auxiliary.AbstractAddGraphElementAction;
import com.topcoder.uml.modelmanager.UMLModelManager;
import com.topcoder.diagraminterchange.GraphElement;
import com.topcoder.diagraminterchange.GraphNode;

/**
 * <p>
 * Extends AbstractAddGraphElementAction. Used for testing.
 * </p>
 *
 * @author wiedzmin
 * @version 1.0
 */
public class AbstractAddGraphElementActionImpl extends AbstractAddGraphElementAction {

    /**
     * <p>
     * Constructs AbstractAddGraphElementAction.
     * </p>
     *  
     * @param name         the presentation name of the undoable action
     * @param child        the child graph element to add to the parent graph node
     * @param parent       the parent graph node the child graph element to add to
     * @param modelManager the UMLModelManager object to apply initial formatting.
     * @throws IllegalArgumentException if any argument is null, or name argument is empty string.
     */
    public AbstractAddGraphElementActionImpl(String name,
                                             GraphElement child,
                                             GraphNode parent,
                                             UMLModelManager modelManager) {
        super(name, child, parent, modelManager);
    }
}
