/*
 * Copyright (C) 2008 TopCoder Inc., All Rights Reserved.
 */
package com.topcoder.gui.closabletabbedpane.accuracytests;

import com.topcoder.gui.closabletabbedpane.ClosableTabbedPaneEvent;

import com.topcoder.gui.closabletabbedpane.ClosableTabbedPaneEventHandler;

/**
 * Mock ClosableTabbedPaneEventHandler class for acc tests.
 *
 * @author knuthocean
 * @version 1.0
 */
public class MockClosableTabbedPaneEventHandlerACC implements ClosableTabbedPaneEventHandler {

    /**
     * <p>
     * Represents the event of a tab having been added to the pane, this is just a does-nothing impl.
     * </p>
     *
     * @param e
     *            The event. Will not be null.
     * @throws IllegalArgumentException
     *             If e is null.
     */
    public void tabAdded(ClosableTabbedPaneEvent e) {
        // empty
    }

    /**
     * <p>
     * Represents the event of tab(s) about to close, this is just a does-nothing impl.
     * </p>
     *
     * @return An array of booleans, with the same length as e. The corresponding element is true to
     *         continue with closure of that tab, false otherwise.
     * @param e
     *            The events, one per tab. Will not be null/empty or contain nulls.
     * @throws IllegalArgumentException
     *             If e is null/empty or contains nulls.
     */
    public boolean[] tabsClosing(ClosableTabbedPaneEvent[] e) {

        // empty
        return null;
    }

    /**
     * <p>
     * Represents the event of tab(s) having closed, this is just a does-nothing impl.
     * </p>
     *
     * @throws IllegalArgumentException
     *             if e is null/empty or contains nulls.
     * @param e
     *            The events, one per tab. Will not be null/empty or contain nulls.
     */
    public void tabsClosed(ClosableTabbedPaneEvent[] e) {
        // empty

    }
}
