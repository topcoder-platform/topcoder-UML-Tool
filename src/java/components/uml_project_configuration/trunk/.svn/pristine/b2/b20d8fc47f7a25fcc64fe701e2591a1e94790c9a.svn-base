package com.topcoder.uml.projectconfiguration.accuracytests.MockObjects;

import com.topcoder.uml.projectconfiguration.InitialDiagramElementFormatter;
import com.topcoder.diagraminterchange.DiagramElement;

/**
 * Mock class of an initial diagram element formatter
 */
public class MockDiagramElementFormatter implements InitialDiagramElementFormatter {

    /**
     * Mock Stub of [format element]
     * @return true always
     */
    public boolean format(DiagramElement elt)    {
        System.out.println("diagram formating called once");
        return true;
    }
}
