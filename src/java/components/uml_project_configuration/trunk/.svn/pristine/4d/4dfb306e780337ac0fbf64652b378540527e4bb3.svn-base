package com.topcoder.uml.projectconfiguration.accuracytests.MockObjects;

import com.topcoder.uml.projectconfiguration.InitialElementFormatter;
import com.topcoder.uml.model.core.ModelElement;

/**
 * Mock class of an initial element formatter
 */
public class MockModelElementFormatter implements InitialElementFormatter {

    /**
     * Mock Stub of [format element]
     * @return true always
     */
    public boolean format(ModelElement elt)    {
        System.out.println("model formating called once");
        return true;
    }
}
